# logstash.yml
http:
  host: "0.0.0.0"
xpack:
  monitoring:
    enabled: true
    elasticsearch:
      username: "elastic"
      password: "myPassword"
      hosts: ["elasticsearch:9200"]
      ssl:
        verification_mode: none
